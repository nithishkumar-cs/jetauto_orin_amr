<?xml version="1.0"?>
<package format="3">
  <name>estop_gate</name>
  <version>0.1.0</version>
  <description>
    Latching software operator-stop gate for the JetAuto AMR. It passes
    velocity commands only when the e-stop is released and explicitly reset,
    then hands them to nav2_collision_monitor for obstacle-zone limiting.
  </description>
  <maintainer email="nithish@example.com">Nithish</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>geometry_msgs</depend>
  <depend>rclcpp</depend>
  <depend>std_msgs</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
