# robot_bringup

System entrypoint package for hardware and replay launch profiles. It composes
platform, perception, diagnostics, safety, and benchmark nodes using package
configs and project-level runtime mode decisions.
