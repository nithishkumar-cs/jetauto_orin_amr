modes:
  webcam_dev:
    description: USB webcam, CUDA preproc debug tensor, detector wrapper, diagnostics.
    depth_required: false
    safe_to_run_on_desk: true
  robot_depth_dev:
    description: Full perception graph with depth projection, tracking, fusion, safety, and base stub.
    depth_required: true
    safe_to_run_on_desk: true
  robot_production:
    description: Full robot graph with hardware base transport, LiDAR/depth, mapping, safety, and benchmarks.
    depth_required: true
    safe_to_run_on_desk: false

