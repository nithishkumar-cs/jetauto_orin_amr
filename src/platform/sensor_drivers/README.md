# sensor_drivers

Controlled access layer for robot sensors. The current implementation includes
a generic RGB camera node and configuration boundaries for the JetAuto depth
camera and LiDAR bring-up.
