jetauto_base_interface_node:
  ros__parameters:
    transport_mode: stub
    cmd_vel_topic: /cmd_vel/safety_limited
    odom_topic: /odom/wheel
    odom_frame: odom
    base_frame: base_link
    cmd_timeout_s: 0.25
    update_rate_hz: 50.0

