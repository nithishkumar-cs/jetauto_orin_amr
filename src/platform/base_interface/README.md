# base_interface

Bridge between the Jetson runtime and the JetAuto base controller. It owns
velocity command forwarding, wheel odometry publication, base health reporting,
and stale controller communication handling.
